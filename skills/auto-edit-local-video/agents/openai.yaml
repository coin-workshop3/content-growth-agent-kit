interface:
  display_name: "本地自动剪辑"
  short_description: "本地转写、废话候选、字幕与两种剪辑"
  default_prompt: "Use $auto-edit-local-video to recommend a mode, optionally transcribe locally, mark review-only filler candidates, create SRT captions, and render a reviewable local draft."
